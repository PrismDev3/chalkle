game-builds/
ugs/
web-port/
cherri-list.txt
/chalkle-single.html
/chalkle-single-cdn.html
sync.json
_served_sync.json
*.log
_chrome*
__pycache__/
*.pyc
node_modules/
stratus-api/node_modules/
music-backend/node_modules/
pack-lock.json
package-lock.json
/tmp/
tmp-*
/keeper.log
_pack*
*.zip
/.vs/
.DS_Store
Thumbs.db

# runtime data / local relay config (never needed by the site itself)
ai_convos.json
cloud-relay.json
music-relay.json

# local dev artifacts / temp profiles (never part of the site)
/_*
**/_*
!/assets/
random-gaming-websites-full.txt
arctic-1m-links-full.txt

# local-only heavy dirs: flare/ is a 5GB local ruffle/flash experiment
# (nothing in the shipped site links into it), screenshots/ is audit output
flare/
screenshots/

# local-only game dirs served from the origin: ugs/ (232MB html stash) and
# mc/ (2.8GB offline minecraft builds)
ugs/
mc/

# local research dumps (1.2GB, not part of the shipped site)
research/
